---
phase: 02-harden-transport-observability
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - tests/test_pipeline_status.py
  - main/pipeline_status.py
  - main/socket_server.py
  - main/ai_analyzer.py
  - main/main.py
  - main/mt5_client.py
autonomous: true
requirements:
  - PIPE-02
user_setup: []
must_haves:
  truths:
    - "Operators can inspect one JSON file and see whether ingest, buffering, inference, and execution are up, degraded, or down."
    - "The status file updates on both state changes and the fixed 5-second heartbeat."
    - "Execution shows `degraded` rather than pretending to be healthy when MT5 is unavailable but the simulator still runs."
  artifacts:
    - path: "tests/test_pipeline_status.py"
      provides: "Regression coverage for JSON shape, atomic publish, and stage transitions"
      min_lines: 40
      contains: "test_status_snapshot_has_only_updated_at_and_session_started_at_top_level_metadata"
    - path: "main/pipeline_status.py"
      provides: "Python-owned stage registry and JSON publisher"
      contains: "\"execution\""
    - path: "main/ai_analyzer.py"
      provides: "Buffering and inference stage updates"
      contains: "model loaded"
    - path: "main/main.py"
      provides: "Execution-stage startup and shutdown reasons"
      contains: "mt5 disconnected; simulator only"
  key_links:
    - from: "main/socket_server.py"
      to: "main/pipeline_status.py"
      via: "ingest state transitions are published to runtime/status.json"
      pattern: "status.update_stage"
    - from: "main/ai_analyzer.py"
      to: "main/pipeline_status.py"
      via: "buffering and inference publish recent activity and failures"
      pattern: "buffering"
    - from: "main/main.py"
      to: "main/mt5_client.py"
      via: "execution stage reports MT5 connectivity vs simulator-only mode"
      pattern: "mt5 disconnected; simulator only"
---

<objective>
Make Python publish one operator-facing health surface for the whole pipeline.

Purpose: `PIPE-02` requires a single source of truth for stage health; logs alone are not enough, and the Phase 2 decisions already lock that source of truth to Python and `runtime/status.json`.
Output: Regression coverage for status behavior plus stage wiring from socket ingest through buffering, inference, and execution.
</objective>

<execution_context>
@C:/Users/hoang/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/hoang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-harden-transport-observability/02-CONTEXT.md
@.planning/phases/02-harden-transport-observability/02-RESEARCH.md
@.planning/phases/02-harden-transport-observability/02-VALIDATION.md
@.planning/phases/02-harden-transport-observability/02-01-PLAN.md
@main/pipeline_status.py
@main/socket_server.py
@main/ai_analyzer.py
@main/main.py
@main/mt5_client.py
@tests/test_socket_lifecycle.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add regression tests for the Phase 2 status surface</name>
  <files>tests/test_pipeline_status.py</files>
  <read_first>tests/test_pipeline_status.py, tests/conftest.py, main/pipeline_status.py, main/socket_server.py, main/ai_analyzer.py, main/main.py, main/mt5_client.py, .planning/phases/02-harden-transport-observability/02-CONTEXT.md</read_first>
  <action>Create or replace `tests/test_pipeline_status.py` with the exact tests `test_status_snapshot_has_only_updated_at_and_session_started_at_top_level_metadata`, `test_status_publish_is_atomic_and_keeps_stage_reason`, `test_stage_state_transitions_use_only_up_degraded_down`, and `test_execution_stage_is_degraded_when_mt5_disconnects_but_simulator_available`. Assert that the only top-level keys are `updated_at`, `session_started_at`, and `stages`; each stage record must contain `state`, `updated_at`, and `reason`; and the only allowed states are `up`, `degraded`, and `down`.</action>
  <verify>
    <automated>python -m pytest tests/test_pipeline_status.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_pipeline_status.py` contains `test_status_snapshot_has_only_updated_at_and_session_started_at_top_level_metadata`
    - `tests/test_pipeline_status.py` contains `test_status_publish_is_atomic_and_keeps_stage_reason`
    - `tests/test_pipeline_status.py` contains `test_stage_state_transitions_use_only_up_degraded_down`
    - `tests/test_pipeline_status.py` contains `test_execution_stage_is_degraded_when_mt5_disconnects_but_simulator_available`
    - `tests/test_pipeline_status.py` contains `updated_at`
    - `tests/test_pipeline_status.py` contains `session_started_at`
  </acceptance_criteria>
  <done>The phase has executable tests for the exact JSON shape and state semantics promised to operators.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ingest, buffering, inference, and execution into `runtime/status.json`</name>
  <files>main/pipeline_status.py, main/socket_server.py, main/ai_analyzer.py, main/main.py, main/mt5_client.py</files>
  <read_first>main/pipeline_status.py, main/socket_server.py, main/ai_analyzer.py, main/main.py, main/mt5_client.py, main/runtime_paths.py, tests/test_pipeline_status.py, .planning/phases/02-harden-transport-observability/02-CONTEXT.md, .planning/phases/02-harden-transport-observability/02-RESEARCH.md</read_first>
  <action>Before editing `AIAnalyzer`, `SocketServer`, and `MT5Client`, run `gitnexus_impact({target: "AIAnalyzer", direction: "upstream", repo: "quant-trading"})`, `gitnexus_impact({target: "SocketServer", direction: "upstream", repo: "quant-trading"})`, and `gitnexus_impact({target: "MT5Client", direction: "upstream", repo: "quant-trading"})`. If GitNexus reports `HIGH` or `CRITICAL`, stop and narrow the change set before continuing. Then make `runtime/status.json` use this exact top-level shape: `updated_at`, `session_started_at`, and `stages`. Under `stages`, publish these exact stage keys: `ingest`, `buffering`, `inference`, `execution`. Each stage must always include `state`, `updated_at`, and `reason`. Add these extra fields under the relevant stage records: `ingest.connected_sources`, `ingest.reconnects_total`, `ingest.dropped_events_total`, `buffering.buffered_symbols`, `buffering.queued_records`, `inference.model_loaded`, `inference.last_inference_at`, `inference.inference_errors_total`, `execution.mt5_connected`, `execution.simulator_enabled`, and `execution.execution_errors_total`. In `AIAnalyzer`, mark buffering as `up` when normalized records are accepted, keep `reason = "awaiting buffered data"` until `_process_buffer` runs, mark inference as `up` with `reason = "model loaded"` after the model is available, and change inference to `degraded` with an incremented `inference_errors_total` on prediction or model-load failures. In `MT5Client`, persist a `last_error` string from `mt5.last_error()` when connects fail. In `main.py`, set `execution.state = "degraded"` and `execution.reason = "mt5 disconnected; simulator only"` when MT5 is unavailable but the `OrderSimulator` still exists, set `execution.state = "up"` and `execution.reason = "mt5 connected"` on success, and mark all stages `down` with `reason = "shutdown"` during orderly shutdown.</action>
  <verify>
    <automated>python -m pytest tests/test_pipeline_status.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `main/pipeline_status.py` contains `"ingest"`
    - `main/pipeline_status.py` contains `"buffering"`
    - `main/pipeline_status.py` contains `"inference"`
    - `main/pipeline_status.py` contains `"execution"`
    - `main/pipeline_status.py` contains `dropped_events_total`
    - `main/ai_analyzer.py` contains `model loaded`
    - `main/main.py` contains `mt5 disconnected; simulator only`
    - `main/main.py` contains `mt5 connected`
    - `main/main.py` contains `shutdown`
    - `main/mt5_client.py` contains `last_error`
  </acceptance_criteria>
  <done>Operators can read one JSON file and understand current stage health, staleness, and execution-mode degradation without reading source code or tailing logs.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/test_pipeline_status.py -q` succeeds
- [ ] `Select-String -Path main\pipeline_status.py -Pattern '"ingest"|"buffering"|"inference"|"execution"|dropped_events_total'` returns matches
- [ ] `Select-String -Path main\main.py -Pattern 'mt5 disconnected; simulator only|mt5 connected|shutdown'` returns matches
- [ ] `gitnexus_detect_changes({scope: "all", repo: "quant-trading"})` reports only the files expected by this plan before commit
- [ ] Manual smoke after implementation: with MT5 unavailable, `runtime/status.json` reports `execution.state = "degraded"` and the simulator-only reason
</verification>

<success_criteria>

- All tasks completed
- All automated verification checks pass
- `runtime/status.json` updates on state changes and the 5-second heartbeat
- The four pipeline stages use only `up`, `degraded`, and `down`
  </success_criteria>

<output>
After completion, create `.planning/phases/02-harden-transport-observability/02-02-SUMMARY.md`
</output>
