---
phase: 02-harden-transport-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pytest.ini
  - tests/conftest.py
  - tests/test_socket_lifecycle.py
  - main/runtime_paths.py
  - main/pipeline_status.py
  - main/socket_server.py
  - main/main.py
  - bookmap-addons/src/main/java/com/bookmap/alertlistener/AlertListener.java
  - ctrader-projects/OrderFlowAggregatedV20/src/Order Flow Aggregated v2.0.Lifecycle.cs
  - ctrader-projects/FreeVolumeProfileV20/src/Free Volume Profile v2.0.Lifecycle.cs
  - ctrader-projects/WeisWyckoffSystemV20/src/Weis & Wyckoff System v2.0.Lifecycle.cs
autonomous: true
requirements:
  - PIPE-01
user_setup: []
must_haves:
  truths:
    - "Bookmap, cTrader, and Python can restart independently without leaving the pipeline permanently disconnected."
    - "Every reconnect sends a fresh `connection_hello` that re-identifies the producer and reports cumulative reconnect/drop counters."
    - "Realtime sends while disconnected are dropped quietly and only cumulative counters grow."
  artifacts:
    - path: "tests/test_socket_lifecycle.py"
      provides: "Regression coverage for startup ordering, reconnects, and drop-counter handoff"
      min_lines: 50
      contains: "test_disconnect_and_reconnect_preserve_cumulative_drop_counters"
    - path: "main/socket_server.py"
      provides: "Python-side reconnect accounting and hello handling"
      contains: "dropped_events_total"
    - path: "bookmap-addons/src/main/java/com/bookmap/alertlistener/AlertListener.java"
      provides: "Forever-retry reconnect loop for the Bookmap producer"
      contains: "reconnect_count"
    - path: "ctrader-projects/OrderFlowAggregatedV20/src/Order Flow Aggregated v2.0.Lifecycle.cs"
      provides: "Throttled reconnect-on-send lifecycle for cTrader exporters"
      contains: "_nextReconnectAtUtc"
  key_links:
    - from: "bookmap-addons/src/main/java/com/bookmap/alertlistener/AlertListener.java"
      to: "main/socket_server.py"
      via: "connection_hello carries dropped_events_total and reconnect_count on every reconnect"
      pattern: "dropped_events_total"
    - from: "ctrader-projects/OrderFlowAggregatedV20/src/Order Flow Aggregated v2.0.Lifecycle.cs"
      to: "main/socket_server.py"
      via: "EnsureSocketConnected and SendConnectionHello restore producer identity after restart"
      pattern: "connection_hello"
    - from: "main/main.py"
      to: "main/pipeline_status.py"
      via: "startup/shutdown lifecycle initializes and publishes ingest state"
      pattern: "PipelineStatus"
---

<objective>
Create the reconnect and lifecycle foundation for Phase 2.

Purpose: `PIPE-01` only becomes reliable if Python owns the lifecycle counters while every producer retries forever, resends identity on reconnect, and stops buffering disconnected realtime traffic.
Output: Shared runtime/status support modules, socket lifecycle regression tests, Python-side reconnect accounting, and producer reconnect state machines for Bookmap plus all three cTrader exporters.
</objective>

<execution_context>
@C:/Users/hoang/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/hoang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-harden-transport-observability/02-CONTEXT.md
@.planning/phases/02-harden-transport-observability/02-RESEARCH.md
@.planning/phases/02-harden-transport-observability/02-VALIDATION.md
@.planning/phases/01-normalize-event-contract/01-02-SUMMARY.md
@.planning/phases/01-normalize-event-contract/01-03-SUMMARY.md
@main/main.py
@main/socket_server.py
@bookmap-addons/src/main/java/com/bookmap/alertlistener/AlertListener.java
@ctrader-projects/OrderFlowAggregatedV20/src/Order Flow Aggregated v2.0.Lifecycle.cs
@ctrader-projects/FreeVolumeProfileV20/src/Free Volume Profile v2.0.Lifecycle.cs
@ctrader-projects/WeisWyckoffSystemV20/src/Weis & Wyckoff System v2.0.Lifecycle.cs

<interfaces>
The reconnect hello payload for this phase must include these exact keys on every successful connect or reconnect:

```json
{
  "kind": "connection_hello",
  "source": "bookmap|ctrader",
  "source_instance": "producer-specific name",
  "instrument": "producer identity or symbol",
  "timestamp": "ISO-8601 UTC timestamp",
  "reconnect_count": 0,
  "dropped_events_total": 0
}
```

`main/pipeline_status.py` should expose one Python-owned registry with four fixed stages: `ingest`, `buffering`, `inference`, `execution`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the lifecycle test harness and shared runtime/status contracts</name>
  <files>pytest.ini, tests/conftest.py, tests/test_socket_lifecycle.py, main/runtime_paths.py, main/pipeline_status.py</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/phases/02-harden-transport-observability/02-CONTEXT.md, .planning/phases/02-harden-transport-observability/02-RESEARCH.md, .planning/phases/02-harden-transport-observability/02-VALIDATION.md, main/main.py, main/socket_server.py</read_first>
  <action>Create `pytest.ini` with `testpaths = tests`, `pythonpath = main`, and `addopts = -q`. Create `tests/conftest.py` with fixtures `runtime_root`, `fake_mt5_client`, `make_connection_hello(source, source_instance, instrument, reconnect_count=0, dropped_events_total=0)`, `make_event(event, instrument, payload=None)`, and helpers that read `runtime/status.json` from a temporary repo-root `runtime/`. Create `tests/test_socket_lifecycle.py` with the exact tests `test_connection_hello_updates_ingest_identity`, `test_disconnect_and_reconnect_preserve_cumulative_drop_counters`, and `test_status_publisher_marks_ingest_degraded_then_down_after_10_and_30_seconds`. Create `main/runtime_paths.py` with helpers `runtime_root()`, `status_file()`, `socket_events_file()`, `quarantine_events_file()`, `trade_history_file()`, `model_file()`, `event_history_file(event_name)`, and `alert_history_file(alias)` that all resolve under one repo-root `runtime/` directory. Create `main/pipeline_status.py` with `DEFAULT_STAGES = ("ingest", "buffering", "inference", "execution")`, `HEARTBEAT_SECONDS = 5`, `DEGRADED_AFTER_SECONDS = 10`, `DOWN_AFTER_SECONDS = 30`, and an atomic `publish()` implementation that writes a temp file in `runtime/` then replaces `runtime/status.json` with `os.replace`.</action>
  <verify>
    <automated>python -m pytest tests --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest.ini` contains `testpaths = tests`
    - `pytest.ini` contains `pythonpath = main`
    - `tests/test_socket_lifecycle.py` contains `test_disconnect_and_reconnect_preserve_cumulative_drop_counters`
    - `main/runtime_paths.py` contains `def status_file`
    - `main/runtime_paths.py` contains `def alert_history_file`
    - `main/pipeline_status.py` contains `DEFAULT_STAGES = ("ingest", "buffering", "inference", "execution")`
    - `main/pipeline_status.py` contains `HEARTBEAT_SECONDS = 5`
    - `main/pipeline_status.py` contains `DOWN_AFTER_SECONDS = 30`
  </acceptance_criteria>
  <done>The repo has a Python test harness plus concrete runtime/status contracts that downstream tasks can implement against without rediscovering filenames or thresholds.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Python socket lifecycle tracking into the shared status registry</name>
  <files>main/socket_server.py, main/main.py, main/pipeline_status.py</files>
  <read_first>main/socket_server.py, main/main.py, main/pipeline_status.py, main/runtime_paths.py, tests/test_socket_lifecycle.py, .planning/phases/02-harden-transport-observability/02-CONTEXT.md</read_first>
  <action>Update `SocketServer` so its constructor accepts `status: PipelineStatus` and an optional runtime-root override for tests. Track connected clients by `source/source_instance`, treat every `connection_hello` as authoritative identity, and read `reconnect_count` plus `dropped_events_total` from the hello payload. On hello, set `ingest.state = "up"`, `ingest.reason = "connected: {source}/{source_instance}"`, and persist the new cumulative counters without resetting them. On disconnect, keep the listener alive but set `ingest.reason = "disconnected: {source}/{source_instance}"`; let the 10-second and 30-second thresholds move the stage from `degraded` to `down` during heartbeats. In `main/main.py`, instantiate `PipelineStatus(session_started_at=...)`, set the initial reasons exactly to `listening; awaiting producers`, `awaiting normalized records`, and `awaiting buffered data`, and publish on startup, every 5-second heartbeat, and orderly shutdown.</action>
  <verify>
    <automated>python -m pytest tests/test_socket_lifecycle.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `main/socket_server.py` contains `reconnect_count`
    - `main/socket_server.py` contains `dropped_events_total`
    - `main/socket_server.py` contains `connected:`
    - `main/socket_server.py` contains `disconnected:`
    - `main/main.py` contains `PipelineStatus(`
    - `main/main.py` contains `listening; awaiting producers`
    - `main/main.py` contains `awaiting normalized records`
    - `main/main.py` contains `awaiting buffered data`
  </acceptance_criteria>
  <done>Python owns the reconnect counters and lifecycle reasons, and the socket listener stays restart-safe instead of treating disconnects as terminal failure.</done>
</task>

<task type="auto">
  <name>Task 3: Convert Bookmap and cTrader producers to forever-retry reconnect state machines</name>
  <files>bookmap-addons/src/main/java/com/bookmap/alertlistener/AlertListener.java, ctrader-projects/OrderFlowAggregatedV20/src/Order Flow Aggregated v2.0.Lifecycle.cs, ctrader-projects/FreeVolumeProfileV20/src/Free Volume Profile v2.0.Lifecycle.cs, ctrader-projects/WeisWyckoffSystemV20/src/Weis & Wyckoff System v2.0.Lifecycle.cs</files>
  <read_first>bookmap-addons/src/main/java/com/bookmap/alertlistener/AlertListener.java, ctrader-projects/OrderFlowAggregatedV20/src/Order Flow Aggregated v2.0.Lifecycle.cs, ctrader-projects/FreeVolumeProfileV20/src/Free Volume Profile v2.0.Lifecycle.cs, ctrader-projects/WeisWyckoffSystemV20/src/Weis & Wyckoff System v2.0.Lifecycle.cs, main/socket_server.py, tests/test_socket_lifecycle.py, .planning/phases/02-harden-transport-observability/02-CONTEXT.md</read_first>
  <action>Refactor `AlertListener.java` so reconnects run through a `ScheduledExecutorService` using `scheduleWithFixedDelay(..., 0, 5, TimeUnit.SECONDS)` instead of the current one-shot background thread. Add cumulative `reconnectCount` and `droppedEventsTotal` fields, log only state transitions `socket disconnected`, `socket reconnecting`, and `socket connected`, and resend `connection_hello` with `reconnect_count` plus `dropped_events_total` after every successful reconnect. Any send path that finds `socketWriter == null` or trips `checkError()` must increment `droppedEventsTotal`, close the socket, and return without logging each dropped event. In all three cTrader lifecycle files, add `_nextReconnectAtUtc`, `_reconnectCount`, `_droppedEventsTotal`, and a string state field such as `_connectionState`. Implement `EnsureSocketConnected()` so it reconnects at most once every 5 seconds, resends `connection_hello` with the same two counters on success, and treats write failures by closing the stream/client, incrementing `_droppedEventsTotal`, and returning silently. Keep the manual Reconnect button wired to the same state machine instead of a separate code path.</action>
  <verify>
    <automated>Select-String -Path "bookmap-addons\src\main\java\com\bookmap\alertlistener\AlertListener.java","ctrader-projects\OrderFlowAggregatedV20\src\Order Flow Aggregated v2.0.Lifecycle.cs","ctrader-projects\FreeVolumeProfileV20\src\Free Volume Profile v2.0.Lifecycle.cs","ctrader-projects\WeisWyckoffSystemV20\src\Weis & Wyckoff System v2.0.Lifecycle.cs" -Pattern "dropped_events_total|reconnect_count|scheduleWithFixedDelay|EnsureSocketConnected|_nextReconnectAtUtc"</automated>
  </verify>
  <acceptance_criteria>
    - `bookmap-addons/src/main/java/com/bookmap/alertlistener/AlertListener.java` contains `scheduleWithFixedDelay`
    - `bookmap-addons/src/main/java/com/bookmap/alertlistener/AlertListener.java` contains `dropped_events_total`
    - `bookmap-addons/src/main/java/com/bookmap/alertlistener/AlertListener.java` contains `reconnect_count`
    - `ctrader-projects/OrderFlowAggregatedV20/src/Order Flow Aggregated v2.0.Lifecycle.cs` contains `EnsureSocketConnected`
    - `ctrader-projects/OrderFlowAggregatedV20/src/Order Flow Aggregated v2.0.Lifecycle.cs` contains `_nextReconnectAtUtc`
    - `ctrader-projects/FreeVolumeProfileV20/src/Free Volume Profile v2.0.Lifecycle.cs` contains `_droppedEventsTotal`
    - `ctrader-projects/WeisWyckoffSystemV20/src/Weis & Wyckoff System v2.0.Lifecycle.cs` contains `_reconnectCount`
  </acceptance_criteria>
  <done>Every producer now reconnects without manual file edits, resends identity on recovery, and converts disconnected sends into cumulative counters instead of noisy logs or hidden buffering.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests --collect-only -q` succeeds
- [ ] `python -m pytest tests/test_socket_lifecycle.py -q` succeeds
- [ ] `Select-String -Path "bookmap-addons\src\main\java\com\bookmap\alertlistener\AlertListener.java","ctrader-projects\OrderFlowAggregatedV20\src\Order Flow Aggregated v2.0.Lifecycle.cs","ctrader-projects\FreeVolumeProfileV20\src\Free Volume Profile v2.0.Lifecycle.cs","ctrader-projects\WeisWyckoffSystemV20\src\Weis & Wyckoff System v2.0.Lifecycle.cs" -Pattern "dropped_events_total|reconnect_count|EnsureSocketConnected"` returns matches
- [ ] Manual smoke after implementation: start Python after producers are already running, then restart Python and confirm a fresh `connection_hello` is observed from Bookmap and at least one cTrader exporter
</verification>

<success_criteria>

- All tasks completed
- All automated verification checks pass
- No producer requires manual file edits to recover from Python startup ordering or disconnects
- Reconnect and drop counters are visible to Python through `connection_hello`
  </success_criteria>

<output>
After completion, create `.planning/phases/02-harden-transport-observability/02-01-SUMMARY.md`
</output>
