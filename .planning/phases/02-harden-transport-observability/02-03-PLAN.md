---
phase: 02-harden-transport-observability
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - tests/test_runtime_artifacts.py
  - main/runtime_paths.py
  - main/socket_server.py
  - main/ai_analyzer.py
  - main/order_simulator.py
  - bookmap-addons/src/main/java/com/bookmap/alertlistener/AlertListener.java
  - .gitignore
  - docs/pipeline-runtime-operations.md
autonomous: true
requirements:
  - PIPE-03
user_setup: []
must_haves:
  truths:
    - "Runtime artifacts land under one flat repo-root `runtime/` directory instead of drifting with the current working directory."
    - "Status, quarantine, socket tap, history, trade, and model files use stable names that operators can predict."
    - "Runtime JSONL and CSV artifacts expose stable key sets and header rows rather than ad hoc per-run schemas."
    - "The repo documents which component owns each runtime artifact and what schema or headers it should contain."
  artifacts:
    - path: "tests/test_runtime_artifacts.py"
      provides: "Regression coverage for exact filenames and runtime-root resolution"
      min_lines: 35
      contains: "test_event_history_file_names_are_exact"
    - path: "main/runtime_paths.py"
      provides: "Single source of truth for runtime filenames"
      contains: "history_order_flow_aggregated.csv"
    - path: "docs/pipeline-runtime-operations.md"
      provides: "Operator-facing runtime file map and ownership rules"
      min_lines: 40
      contains: "runtime/status.json"
    - path: ".gitignore"
      provides: "Repo-level ignore rule for runtime artifacts"
      contains: "runtime/"
  key_links:
    - from: "main/socket_server.py"
      to: "main/runtime_paths.py"
      via: "quarantine and socket tap logs resolve through the runtime-path helper"
      pattern: "socket_events_file"
    - from: "main/ai_analyzer.py"
      to: "main/runtime_paths.py"
      via: "history and model outputs use stable event-to-filename mapping"
      pattern: "history_order_flow_aggregated.csv"
    - from: "docs/pipeline-runtime-operations.md"
      to: "main/runtime_paths.py"
      via: "documented filenames match the code-owned runtime path constants"
      pattern: "runtime/status.json"
---

<objective>
Normalize runtime artifact paths, names, and ownership for live troubleshooting.

Purpose: `PIPE-03` fails if files still land in cwd-relative or user-home-relative locations, or if operators have to guess which process owns a given CSV, JSONL, or model artifact.
Output: Runtime artifact regression tests, Python call sites routed through one path module, Bookmap alert-history ownership clarified, and one doc that maps stable filenames to owners.
</objective>

<execution_context>
@C:/Users/hoang/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/hoang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-harden-transport-observability/02-CONTEXT.md
@.planning/phases/02-harden-transport-observability/02-RESEARCH.md
@.planning/phases/02-harden-transport-observability/02-VALIDATION.md
@.planning/phases/02-harden-transport-observability/02-01-PLAN.md
@.planning/phases/02-harden-transport-observability/02-02-PLAN.md
@main/runtime_paths.py
@main/socket_server.py
@main/ai_analyzer.py
@main/order_simulator.py
@bookmap-addons/src/main/java/com/bookmap/alertlistener/AlertListener.java
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add regression tests for exact runtime filenames and repo-root resolution</name>
  <files>tests/test_runtime_artifacts.py</files>
  <read_first>tests/test_runtime_artifacts.py, tests/conftest.py, main/runtime_paths.py, main/socket_server.py, main/ai_analyzer.py, main/order_simulator.py, .planning/phases/02-harden-transport-observability/02-RESEARCH.md</read_first>
  <action>Create or replace `tests/test_runtime_artifacts.py` with these exact tests: `test_runtime_paths_resolve_repo_root_runtime_directory`, `test_event_history_file_names_are_exact`, `test_order_simulator_defaults_to_runtime_trade_history`, `test_status_and_quarantine_paths_are_flat`, `test_socket_events_jsonl_schema_is_exact`, `test_quarantine_events_jsonl_schema_is_exact`, `test_history_csv_headers_are_exact`, and `test_trade_history_headers_are_exact`. Assert these exact filenames under the repo-root `runtime/` directory: `status.json`, `socket_events.jsonl`, `quarantine_events.jsonl`, `history_order_flow_aggregated.csv`, `history_volume_profile.csv`, `history_wyckoff_state.csv`, `history_alert_<alias>.csv`, `trade_history.csv`, and `model.pkl`. For schema/header assertions, require `socket_events.jsonl` records to have exactly `received_at`, `client`, `raw`, `normalized`; require `quarantine_events.jsonl` records to have exactly `received_at`, `reason`, `raw`; require `history_order_flow_aggregated.csv` headers to be exactly `timestamp,instrument,symbol,source,source_instance,event,deltaRank,volumesRank,volumesRankUp,volumesRankDown,spread`; require `history_volume_profile.csv` headers to be exactly `timestamp,instrument,symbol,source,source_instance,event,profile_type,vpPOC,vpVAH,vpVAL,spread`; require `history_wyckoff_state.csv` headers to be exactly `timestamp,instrument,symbol,source,source_instance,event,wyckoffVolume,wyckoffTime,zigZag,waveVolume,wavePrice,waveVolPrice,waveDirection,spread`; require `history_alert_<alias>.csv` headers to be exactly `Timestamp,AlertNumber,Symbol,AlertName,Value,Price,Popup,RawText`; and require `trade_history.csv` headers to be exactly `timestamp,symbol,direction,entry_price,sl_price,tp_price,lot_size,exit_price,exit_reason,pnl_pips,pnl_dollar,pnl_percent,balance_before,balance_after`.</action>
  <verify>
    <automated>python -m pytest tests/test_runtime_artifacts.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_runtime_artifacts.py` contains `test_runtime_paths_resolve_repo_root_runtime_directory`
    - `tests/test_runtime_artifacts.py` contains `test_event_history_file_names_are_exact`
    - `tests/test_runtime_artifacts.py` contains `test_order_simulator_defaults_to_runtime_trade_history`
    - `tests/test_runtime_artifacts.py` contains `test_socket_events_jsonl_schema_is_exact`
    - `tests/test_runtime_artifacts.py` contains `test_quarantine_events_jsonl_schema_is_exact`
    - `tests/test_runtime_artifacts.py` contains `test_history_csv_headers_are_exact`
    - `tests/test_runtime_artifacts.py` contains `test_trade_history_headers_are_exact`
    - `tests/test_runtime_artifacts.py` contains `history_order_flow_aggregated.csv`
    - `tests/test_runtime_artifacts.py` contains `history_volume_profile.csv`
    - `tests/test_runtime_artifacts.py` contains `history_wyckoff_state.csv`
    - `tests/test_runtime_artifacts.py` contains `trade_history.csv`
    - `tests/test_runtime_artifacts.py` contains `received_at`
    - `tests/test_runtime_artifacts.py` contains `balance_after`
  </acceptance_criteria>
  <done>The phase has executable checks for the exact runtime filenames and directory layout it promises.</done>
</task>

<task type="auto">
  <name>Task 2: Route Python runtime outputs through one stable path map</name>
  <files>main/runtime_paths.py, main/socket_server.py, main/ai_analyzer.py, main/order_simulator.py</files>
  <read_first>main/runtime_paths.py, main/socket_server.py, main/ai_analyzer.py, main/order_simulator.py, tests/test_runtime_artifacts.py, .planning/phases/02-harden-transport-observability/02-CONTEXT.md, .planning/phases/02-harden-transport-observability/02-RESEARCH.md</read_first>
  <action>Before editing `SocketServer`, `AIAnalyzer`, and `OrderSimulator`, run `gitnexus_impact({target: "SocketServer", direction: "upstream", repo: "quant-trading"})`, `gitnexus_impact({target: "AIAnalyzer", direction: "upstream", repo: "quant-trading"})`, and `gitnexus_impact({target: "OrderSimulator", direction: "upstream", repo: "quant-trading"})`. Then make `main/runtime_paths.py` the only filename authority and use these exact runtime filenames: `runtime/status.json`, `runtime/socket_events.jsonl`, `runtime/quarantine_events.jsonl`, `runtime/history_order_flow_aggregated.csv`, `runtime/history_volume_profile.csv`, `runtime/history_wyckoff_state.csv`, `runtime/history_alert_<alias>.csv`, `runtime/trade_history.csv`, and `runtime/model.pkl`. Update `main/socket_server.py` so `socket_events.jsonl` writes exactly these keys per record: `received_at`, `client`, `raw`, `normalized`; and `quarantine_events.jsonl` writes exactly these keys per record: `received_at`, `reason`, `raw`. Update `AIAnalyzer` so the default `model_path` is `runtime/model.pkl`, the indicator exporters stop using dynamic CSV headers, and the following exact header rows are always written in this order: `history_order_flow_aggregated.csv` -> `timestamp,instrument,symbol,source,source_instance,event,deltaRank,volumesRank,volumesRankUp,volumesRankDown,spread`; `history_volume_profile.csv` -> `timestamp,instrument,symbol,source,source_instance,event,profile_type,vpPOC,vpVAH,vpVAL,spread`; `history_wyckoff_state.csv` -> `timestamp,instrument,symbol,source,source_instance,event,wyckoffVolume,wyckoffTime,zigZag,waveVolume,wavePrice,waveVolPrice,waveDirection,spread`; `history_alert_<alias>.csv` -> `Timestamp,AlertNumber,Symbol,AlertName,Value,Price,Popup,RawText`. Update `OrderSimulator` so the default `trade_log` path is `runtime/trade_history.csv` and the exact trade-history header row is `timestamp,symbol,direction,entry_price,sl_price,tp_price,lot_size,exit_price,exit_reason,pnl_pips,pnl_dollar,pnl_percent,balance_before,balance_after`.</action>
  <verify>
    <automated>python -m pytest tests/test_runtime_artifacts.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `main/runtime_paths.py` contains `history_order_flow_aggregated.csv`
    - `main/runtime_paths.py` contains `history_volume_profile.csv`
    - `main/runtime_paths.py` contains `history_wyckoff_state.csv`
    - `main/runtime_paths.py` contains `trade_history.csv`
    - `main/runtime_paths.py` contains `model.pkl`
    - `main/socket_server.py` contains `socket_events_file(`
    - `main/socket_server.py` contains `quarantine_events_file(`
    - `main/socket_server.py` contains `normalized`
    - `main/ai_analyzer.py` contains `history_alert_`
    - `main/ai_analyzer.py` contains `history_order_flow_aggregated.csv`
    - `main/ai_analyzer.py` contains `history_volume_profile.csv`
    - `main/ai_analyzer.py` contains `history_wyckoff_state.csv`
    - `main/order_simulator.py` contains `runtime/trade_history.csv`
    - `main/order_simulator.py` contains `balance_after`
  </acceptance_criteria>
  <done>Python runtime artifacts stop drifting by process cwd and become predictable enough for replay, diagnosis, and later automation.</done>
</task>

<task type="auto">
  <name>Task 3: Document runtime artifact ownership and remove duplicate Bookmap history naming</name>
  <files>bookmap-addons/src/main/java/com/bookmap/alertlistener/AlertListener.java, .gitignore, docs/pipeline-runtime-operations.md</files>
  <read_first>bookmap-addons/src/main/java/com/bookmap/alertlistener/AlertListener.java, .gitignore, docs/pipeline-runtime-operations.md, main/runtime_paths.py, main/ai_analyzer.py, .planning/phases/02-harden-transport-observability/02-CONTEXT.md</read_first>
  <action>Before editing `AlertListener`, run `gitnexus_impact({target: "AlertListener", direction: "upstream", repo: "quant-trading", file_path: "bookmap-addons/src/main/java/com/bookmap/alertlistener/AlertListener.java"})`. Then stop `AlertListener.java` from creating `AlertListener_history_system.csv`, `AlertListener_history_<alias>.csv`, or any other duplicate alert-history filenames. Python's `AIAnalyzer.export_alert_csv()` is the sole owner of `runtime/history_alert_<alias>.csv` in this phase. Keep Bookmap-side UI logging, reconnect status, and socket transport, but remove file writes or legacy filename helpers that would reintroduce alternate alert-history artifacts. Add `runtime/` to `.gitignore` so `status.json` and the flat runtime files stay out of git. Create `docs/pipeline-runtime-operations.md` and document the exact filenames, owning component, format (`JSON`, `JSONL`, `CSV`, binary), the exact JSONL key sets from Task 2 (`received_at, client, raw, normalized` and `received_at, reason, raw`), the fixed header rows for the four CSV families plus `trade_history.csv`, and the rule that the top-level metadata in `runtime/status.json` is only `updated_at` and `session_started_at`.</action>
  <verify>
    <automated>Select-String -Path ".gitignore","docs\pipeline-runtime-operations.md" -Pattern "runtime/|runtime/status.json|socket_events.jsonl|quarantine_events.jsonl|history_alert_"; if (Select-String -Path "bookmap-addons\src\main\java\com\bookmap\alertlistener\AlertListener.java" -Pattern "AlertListener_history_") { exit 1 }</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` contains `runtime/`
    - `docs/pipeline-runtime-operations.md` contains `runtime/status.json`
    - `docs/pipeline-runtime-operations.md` contains `socket_events.jsonl`
    - `docs/pipeline-runtime-operations.md` contains `quarantine_events.jsonl`
    - `docs/pipeline-runtime-operations.md` contains `history_order_flow_aggregated.csv`
    - `docs/pipeline-runtime-operations.md` contains `history_volume_profile.csv`
    - `docs/pipeline-runtime-operations.md` contains `history_wyckoff_state.csv`
    - `docs/pipeline-runtime-operations.md` contains `received_at, client, raw, normalized`
    - `docs/pipeline-runtime-operations.md` contains `timestamp,symbol,direction,entry_price`
    - `bookmap-addons/src/main/java/com/bookmap/alertlistener/AlertListener.java` does not contain `AlertListener_history_`
  </acceptance_criteria>
  <done>The repo declares one owner for each runtime artifact, one runtime directory, and no duplicate Bookmap alert-history filenames.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/test_runtime_artifacts.py -q` succeeds
- [ ] `Select-String -Path main\runtime_paths.py -Pattern 'status.json|socket_events.jsonl|quarantine_events.jsonl|history_order_flow_aggregated.csv|history_volume_profile.csv|history_wyckoff_state.csv|trade_history.csv|model.pkl'` returns matches
- [ ] `Select-String -Path .gitignore,docs\pipeline-runtime-operations.md -Pattern 'runtime/|runtime/status.json|socket_events.jsonl|quarantine_events.jsonl'` returns matches
- [ ] `if (Select-String -Path "bookmap-addons\src\main\java\com\bookmap\alertlistener\AlertListener.java" -Pattern 'AlertListener_history_') { exit 1 }` exits `0`
- [ ] `gitnexus_detect_changes({scope: "all", repo: "quant-trading"})` reports only the files expected by this plan before commit
- [ ] Manual smoke after implementation: run the Python bridge once and confirm every created live artifact lands under `runtime\` instead of the repo root or user home
</verification>

<success_criteria>

- All tasks completed
- All automated verification checks pass
- Runtime files use one flat repo-root layout with stable names
- Operators have one in-repo reference that maps each runtime file to its owner and format
  </success_criteria>

<output>
After completion, create `.planning/phases/02-harden-transport-observability/02-03-SUMMARY.md`
</output>
