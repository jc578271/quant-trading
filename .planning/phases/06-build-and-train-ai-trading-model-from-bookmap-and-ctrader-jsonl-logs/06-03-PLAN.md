---
phase: 06-build-and-train-ai-trading-model-from-bookmap-and-ctrader-jsonl-logs
plan: 03
type: execute
wave: 3
depends_on:
  - 02
files_modified:
  - main/runtime_paths.py
  - main/ai_analyzer.py
  - main/training/promote_model.py
  - scripts/promote_model.py
  - tests/test_model_promotion.py
  - docs/ai-model-training.md
  - docs/pipeline-runtime-operations.md
autonomous: false
requirements:
  - MLTRAIN-02
  - MLEVAL-01
user_setup: []
must_haves:
  truths:
    - "Offline training and runtime activation are separate actions."
    - "Runtime refuses incompatible model artifacts instead of silently bootstrapping a dummy model."
    - "If the promoted schema includes Bookmap-derived columns, runtime extracts those columns through the same canonical market map used offline."
    - "Operators have one documented flow for build -> train -> promote -> verify."
  artifacts:
    - path: "main/runtime_paths.py"
      provides: "Runtime manifest path next to `runtime/model.pkl`"
      contains: "model_manifest_file"
    - path: "scripts/promote_model.py"
      provides: "Explicit promotion command for one blessed artifact run"
      contains: "--run-id"
    - path: "tests/test_model_promotion.py"
      provides: "Regression coverage for promotion and runtime compatibility checks"
      contains: "test_ai_analyzer_rejects_manifest_with_wrong_feature_schema"
  key_links:
    - from: "artifacts/models/ctrader_xauusd_h1_baseline/{run_id}/model.pkl"
      to: "runtime/model.pkl"
      via: "Only explicit promotion changes the live runtime model"
      pattern: "model.pkl"
    - from: "artifacts/models/ctrader_xauusd_h1_baseline/{run_id}/feature_schema.json"
      to: "main/ai_analyzer.py"
      via: "Runtime loads only manifests that match the expected feature schema"
      pattern: "feature_schema"
---

<objective>
Add explicit runtime promotion and compatibility checks for the Phase 6 baseline model.

Purpose: Phase 6 is incomplete if training ends at offline artifacts and runtime inference still relies on dummy bootstrap behavior or implicit artifact selection.
Output: one explicit promotion CLI, runtime manifest handling, AIAnalyzer schema checks, and updated operator docs.
</objective>

<execution_context>
@C:/Users/hoang/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/hoang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-build-and-train-ai-trading-model-from-bookmap-and-ctrader-jsonl-logs/06-RESEARCH.md
@.planning/phases/06-build-and-train-ai-trading-model-from-bookmap-and-ctrader-jsonl-logs/06-VALIDATION.md
@.planning/phases/06-build-and-train-ai-trading-model-from-bookmap-and-ctrader-jsonl-logs/06-02-PLAN.md
@main/ai_analyzer.py
@main/runtime_paths.py
@tests/test_runtime_artifacts.py
@docs/ai-model-training.md
@docs/pipeline-runtime-operations.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit artifact promotion and runtime manifest paths</name>
  <files>main/runtime_paths.py, main/training/promote_model.py, scripts/promote_model.py, tests/test_model_promotion.py, docs/ai-model-training.md, docs/pipeline-runtime-operations.md</files>
  <read_first>main/runtime_paths.py, tests/test_runtime_artifacts.py, docs/ai-model-training.md, docs/pipeline-runtime-operations.md, .planning/phases/06-build-and-train-ai-trading-model-from-bookmap-and-ctrader-jsonl-logs/06-RESEARCH.md</read_first>
  <action>Update `main/runtime_paths.py` to add `MODEL_MANIFEST_FILE_NAME = "model_manifest.json"` and `def model_manifest_file() -> Path`. Create `main/training/promote_model.py` and `scripts/promote_model.py` with the exact CLI `python scripts/promote_model.py --scope ctrader_xauusd_h1_baseline --run-id <run_id> --artifacts-root artifacts/models --runtime-root runtime`. The promotion logic must copy only `model.pkl`, `feature_schema.json`, `train_config.json`, and `dataset_manifest.json` metadata into runtime-visible files, with the manifest written to `runtime/model_manifest.json` and containing `dataset_scope`, `run_id`, `feature_schema_path`, `label_spec`, and `created_at`. Create `tests/test_model_promotion.py` with exact tests `test_promotion_copies_blessed_artifact_into_runtime`, `test_promotion_does_not_train_new_model`, and `test_runtime_paths_expose_model_manifest_file`. Update `docs/ai-model-training.md` and `docs/pipeline-runtime-operations.md` so both explicitly say that `runtime/model.pkl` is a promoted artifact and that raw histories remain under `logs/*.jsonl`.</action>
  <verify>
    <automated>& "$env:LOCALAPPDATA\Programs\Python\Python312\python.exe" -m pytest tests/test_model_promotion.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `main/runtime_paths.py` contains `MODEL_MANIFEST_FILE_NAME = "model_manifest.json"`
    - `main/runtime_paths.py` contains `def model_manifest_file`
    - `scripts/promote_model.py` contains `--scope ctrader_xauusd_h1_baseline`
    - `scripts/promote_model.py` contains `runtime/model_manifest.json`
    - `tests/test_model_promotion.py` contains `test_runtime_paths_expose_model_manifest_file`
    - `docs/pipeline-runtime-operations.md` contains `runtime/model.pkl is a promoted artifact`
  </acceptance_criteria>
  <done>The repo has one explicit promotion flow and one runtime manifest path for activated models.</done>
</task>

<task type="auto">
  <name>Task 2: Make AIAnalyzer load only compatible promoted artifacts</name>
  <files>main/ai_analyzer.py, tests/test_model_promotion.py</files>
  <read_first>main/ai_analyzer.py, main/runtime_paths.py, main/training/promote_model.py, tests/test_runtime_artifacts.py, .planning/phases/06-build-and-train-ai-trading-model-from-bookmap-and-ctrader-jsonl-logs/06-RESEARCH.md</read_first>
  <action>Before editing `AIAnalyzer`, run `gitnexus_impact({target: "AIAnalyzer", direction: "upstream", repo: "quant-trading"})` and record the direct callers, affected processes, and risk level in the execution notes. If the risk rises above `MEDIUM`, stop and narrow the change before continuing. Then update `main/ai_analyzer.py` so `_load_or_train_initial_model()` first looks for `runtime/model.pkl` plus `runtime/model_manifest.json`. It must refuse to load the artifact when the manifest `dataset_scope` is not `ctrader_xauusd_h1_baseline` or when the manifest feature schema is not exactly `["level_count", "total_volume", "buy_volume", "sell_volume", "delta_sum", "abs_delta_sum", "poc_price", "poc_distance_to_close_ticks", "wy_count", "wy_last_direction_sign", "wy_last_wave_volume", "wy_sum_wave_volume", "wy_mean_volume_per_time", "wy_max_wave_efficiency", "has_volume_profile", "has_wyckoff_window", "has_bookmap_window", "bookmap_event_count_300s", "bookmap_dot_count_300s", "bookmap_stop_count_300s", "bookmap_wall_count_300s", "bookmap_signed_value_300s"]`. Add runtime feature extraction support for those Bookmap fields by maintaining a 300-second in-memory Bookmap event window keyed by canonical market, mapping `GC*` aliases into `XAUUSD` and `ES*` aliases into `US500`, and deriving signed value from Bookmap payload `value` with `BUY/ASK` treated as positive and `SELL/BID` treated as negative. When no promoted artifact exists, publish inference status reason `training artifact missing` and do not silently write a new dummy runtime model. If a bootstrap fallback is still needed for local smoke only, gate it behind the exact env var `QT_ALLOW_BOOTSTRAP_MODEL=1`. Extend `tests/test_model_promotion.py` with exact tests `test_ai_analyzer_rejects_manifest_with_wrong_dataset_scope`, `test_ai_analyzer_rejects_manifest_with_wrong_feature_schema`, `test_ai_analyzer_reports_training_artifact_missing_without_bootstrap_env`, and `test_ai_analyzer_maps_gc_aliases_into_xauusd_bookmap_window`.</action>
  <verify>
    <automated>& "$env:LOCALAPPDATA\Programs\Python\Python312\python.exe" -m pytest tests/test_model_promotion.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `main/ai_analyzer.py` contains `training artifact missing`
    - `main/ai_analyzer.py` contains `QT_ALLOW_BOOTSTRAP_MODEL`
    - `main/ai_analyzer.py` contains `ctrader_xauusd_h1_baseline`
    - `main/ai_analyzer.py` contains `wy_max_wave_efficiency`
    - `main/ai_analyzer.py` contains `bookmap_event_count_300s`
    - `main/ai_analyzer.py` contains `bookmap_signed_value_300s`
    - `tests/test_model_promotion.py` contains `test_ai_analyzer_rejects_manifest_with_wrong_dataset_scope`
    - `tests/test_model_promotion.py` contains `test_ai_analyzer_rejects_manifest_with_wrong_feature_schema`
    - `tests/test_model_promotion.py` contains `test_ai_analyzer_maps_gc_aliases_into_xauusd_bookmap_window`
  </acceptance_criteria>
  <done>Runtime loads only explicit compatible promoted artifacts and fails visibly when promotion is missing or incompatible.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `& "$env:LOCALAPPDATA\Programs\Python\Python312\python.exe" -m pytest tests/test_model_promotion.py -q` succeeds
- [ ] `python scripts/promote_model.py --scope ctrader_xauusd_h1_baseline --run-id <run_id> --artifacts-root artifacts/models --runtime-root runtime` succeeds for a real artifact run
- [ ] `runtime/model_manifest.json` exists after promotion
- [ ] Starting `main/main.py` after promotion uses the promoted artifact rather than writing a fresh dummy `runtime/model.pkl`
- [ ] `gitnexus_detect_changes({scope: "all", repo: "quant-trading"})` reports only the files expected by this plan before commit
</verification>

<success_criteria>
- All tasks completed
- Promotion and runtime activation are separate steps
- AIAnalyzer rejects incompatible artifacts visibly
- Operator docs reflect the real JSONL/raw-history and promoted-model flow
</success_criteria>

<output>
After completion, create `.planning/phases/06-build-and-train-ai-trading-model-from-bookmap-and-ctrader-jsonl-logs/06-03-SUMMARY.md`
</output>
