---
phase: 06-build-and-train-ai-trading-model-from-bookmap-and-ctrader-jsonl-logs
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - configs/training/ctrader_xauusd_baseline.json
  - main/training/labels.py
  - main/training/train_model.py
  - scripts/train_model.py
  - tests/test_training_labels.py
  - tests/test_training_pipeline.py
  - docs/ai-model-training.md
autonomous: false
requirements:
  - MLLABEL-01
  - MLTRAIN-01
  - MLEVAL-01
user_setup: []
must_haves:
  truths:
    - "Labels are derived only from future movement on the anchor order-flow series."
    - "Offline training writes one versioned artifact bundle per run under `artifacts/models/`."
    - "Evaluation is walk-forward and reproducible rather than shuffled or ad hoc, and the feature schema already reserves fixed Bookmap-derived columns when mapped data is available."
  artifacts:
    - path: "configs/training/ctrader_xauusd_baseline.json"
      provides: "Locked baseline label/eval settings for the first promoted model"
      contains: "\"horizon_bars\": 1"
    - path: "main/training/labels.py"
      provides: "Deterministic future-return and class-label generation"
      contains: "future_return_ticks_1"
    - path: "scripts/train_model.py"
      provides: "CLI that trains and evaluates the baseline model"
      contains: "artifacts/models"
    - path: "tests/test_training_pipeline.py"
      provides: "Regression coverage for artifact bundles and walk-forward evaluation"
      contains: "test_train_run_writes_model_metrics_and_manifest"
  key_links:
    - from: "artifacts/datasets/ctrader_xauusd_h1_baseline/{run_id}/training_rows.parquet"
      to: "scripts/train_model.py"
      via: "Offline training consumes the derived dataset instead of raw JSONL files"
      pattern: "training_rows.parquet"
    - from: "configs/training/ctrader_xauusd_baseline.json"
      to: "main/training/labels.py"
      via: "Label thresholds and horizons are locked in one baseline config"
      pattern: "horizon_bars"
---

<objective>
Train and evaluate the first deterministic offline baseline model for Phase 6.

Purpose: the phase only becomes real once the derived dataset can produce a versioned model artifact, metrics, and replayable predictions without touching live runtime files.
Output: baseline training config, deterministic labels, walk-forward evaluation, and one artifact bundle under `artifacts/models/`.
</objective>

<execution_context>
@C:/Users/hoang/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/hoang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-build-and-train-ai-trading-model-from-bookmap-and-ctrader-jsonl-logs/06-RESEARCH.md
@.planning/phases/06-build-and-train-ai-trading-model-from-bookmap-and-ctrader-jsonl-logs/06-VALIDATION.md
@.planning/phases/06-build-and-train-ai-trading-model-from-bookmap-and-ctrader-jsonl-logs/06-01-PLAN.md
@main/requirements.txt
@main/training/contracts.py
@main/training/build_dataset.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock the baseline label spec and training config</name>
  <files>configs/training/ctrader_xauusd_baseline.json, main/training/labels.py, tests/test_training_labels.py</files>
  <read_first>.planning/phases/06-build-and-train-ai-trading-model-from-bookmap-and-ctrader-jsonl-logs/06-RESEARCH.md, main/training/contracts.py, main/training/build_dataset.py</read_first>
  <action>Create `configs/training/ctrader_xauusd_baseline.json` with these exact baseline values: `dataset_scope = "ctrader_xauusd_h1_baseline"`, `label.type = "future_return_classification"`, `label.horizon_bars = 1`, `label.long_threshold_ticks = 150`, `label.short_threshold_ticks = -150`, `evaluation.method = "walk_forward"`, `evaluation.n_splits = 3`, `model.type = "random_forest_classifier"`, `model.n_estimators = 200`, and `model.random_state = 42`. Create `main/training/labels.py` with helpers that compute `future_return_ticks_1 = (close[t+1] - close[t]) / tick_size` and `target_class_1` using the exact thresholds above. Create `tests/test_training_labels.py` with exact tests `test_future_return_ticks_uses_next_anchor_close_only`, `test_target_class_uses_locked_tick_thresholds`, and `test_label_builder_drops_rows_without_future_anchor_bar`.</action>
  <verify>
    <automated>& "$env:LOCALAPPDATA\Programs\Python\Python312\python.exe" -m pytest tests/test_training_labels.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `configs/training/ctrader_xauusd_baseline.json` contains `"horizon_bars": 1`
    - `configs/training/ctrader_xauusd_baseline.json` contains `"long_threshold_ticks": 150`
    - `configs/training/ctrader_xauusd_baseline.json` contains `"short_threshold_ticks": -150`
    - `main/training/labels.py` contains `future_return_ticks_1`
    - `main/training/labels.py` contains `target_class_1`
    - `tests/test_training_labels.py` contains `test_target_class_uses_locked_tick_thresholds`
  </acceptance_criteria>
  <done>The first promoted model has one locked label policy and one locked training config.</done>
</task>

<task type="auto">
  <name>Task 2: Train the baseline model with walk-forward evaluation and versioned artifacts</name>
  <files>main/training/train_model.py, scripts/train_model.py, tests/test_training_pipeline.py, docs/ai-model-training.md</files>
  <read_first>configs/training/ctrader_xauusd_baseline.json, main/training/labels.py, main/training/build_dataset.py, main/requirements.txt, .planning/phases/06-build-and-train-ai-trading-model-from-bookmap-and-ctrader-jsonl-logs/06-RESEARCH.md</read_first>
  <action>Create `main/training/train_model.py` so it loads the derived parquet dataset for `ctrader_xauusd_h1_baseline`, applies the locked label builder, evaluates with `TimeSeriesSplit(n_splits=3)`, trains `RandomForestClassifier(n_estimators=200, random_state=42, class_weight="balanced_subsample")`, and writes one versioned run under `artifacts/models/ctrader_xauusd_h1_baseline/{run_id}/`. The run directory must contain exactly `model.pkl`, `metrics.json`, `predictions.parquet`, `train_config.json`, `dataset_manifest.json`, and `feature_schema.json`. Create `scripts/train_model.py` with the CLI `python scripts/train_model.py --config configs/training/ctrader_xauusd_baseline.json --dataset-root artifacts/datasets --output-root artifacts/models`. The metrics file must include `train_rows`, `validation_rows`, `test_rows`, `macro_f1`, and `balanced_accuracy`. The feature schema file must list the exact feature order `["level_count", "total_volume", "buy_volume", "sell_volume", "delta_sum", "abs_delta_sum", "poc_price", "poc_distance_to_close_ticks", "wy_count", "wy_last_direction_sign", "wy_last_wave_volume", "wy_sum_wave_volume", "wy_mean_volume_per_time", "wy_max_wave_efficiency", "has_volume_profile", "has_wyckoff_window", "has_bookmap_window", "bookmap_event_count_300s", "bookmap_dot_count_300s", "bookmap_stop_count_300s", "bookmap_wall_count_300s", "bookmap_signed_value_300s"]`. Create `tests/test_training_pipeline.py` with exact tests `test_train_run_writes_model_metrics_and_manifest`, `test_walk_forward_evaluation_preserves_time_order`, `test_training_run_keeps_runtime_model_untouched`, and `test_feature_schema_keeps_bookmap_columns_even_when_current_sample_is_unmatched`. Update `docs/ai-model-training.md` with the exact build command, train command, artifact directory layout, the canonical map examples `XAUUSD -> GC` and `US500 -> ES`, and the warning that training must never write directly to `runtime/model.pkl`.</action>
  <verify>
    <automated>& "$env:LOCALAPPDATA\Programs\Python\Python312\python.exe" -m pytest tests/test_training_pipeline.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `main/training/train_model.py` contains `TimeSeriesSplit(n_splits=3)`
    - `main/training/train_model.py` contains `RandomForestClassifier(n_estimators=200, random_state=42, class_weight="balanced_subsample")`
    - `scripts/train_model.py` contains `artifacts/models/ctrader_xauusd_h1_baseline`
    - `scripts/train_model.py` contains `predictions.parquet`
    - `tests/test_training_pipeline.py` contains `test_training_run_keeps_runtime_model_untouched`
    - `tests/test_training_pipeline.py` contains `test_feature_schema_keeps_bookmap_columns_even_when_current_sample_is_unmatched`
    - `main/training/train_model.py` contains `bookmap_event_count_300s`
    - `docs/ai-model-training.md` contains `training must never write directly to runtime/model.pkl`
  </acceptance_criteria>
  <done>The repo can train one deterministic offline baseline and emit a versioned artifact bundle without mutating runtime state.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `& "$env:LOCALAPPDATA\Programs\Python\Python312\python.exe" -m pytest tests/test_training_labels.py -q` succeeds
- [ ] `& "$env:LOCALAPPDATA\Programs\Python\Python312\python.exe" -m pytest tests/test_training_pipeline.py -q` succeeds
- [ ] `python scripts/train_model.py --config configs/training/ctrader_xauusd_baseline.json --dataset-root artifacts/datasets --output-root artifacts/models` succeeds
- [ ] The latest run under `artifacts/models/ctrader_xauusd_h1_baseline/{run_id}/` contains `model.pkl`, `metrics.json`, `train_config.json`, `dataset_manifest.json`, `feature_schema.json`, and `predictions.parquet`
- [ ] `runtime/model.pkl` is unchanged until promotion
- [ ] `gitnexus_detect_changes({scope: "all", repo: "quant-trading"})` reports only the files expected by this plan before commit
</verification>

<success_criteria>
- All tasks completed
- Labels are future-only and deterministic
- Evaluation is walk-forward and reproducible
- Offline training produces a versioned artifact bundle without touching runtime
</success_criteria>

<output>
After completion, create `.planning/phases/06-build-and-train-ai-trading-model-from-bookmap-and-ctrader-jsonl-logs/06-02-SUMMARY.md`
</output>
