---
phase: 06-build-and-train-ai-trading-model-from-bookmap-and-ctrader-jsonl-logs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - configs/training/instrument_map.json
  - docs/ai-model-data-contract.md
  - main/training/__init__.py
  - main/training/contracts.py
  - main/training/build_dataset.py
  - scripts/build_training_dataset.py
  - tests/test_training_dataset_contract.py
  - tests/test_training_join_policy.py
autonomous: false
requirements:
  - MLDATA-01
  - MLDATA-02
user_setup: []
must_haves:
  truths:
    - "Phase 6 has explicit requirement IDs and no longer hides behind `Requirements: TBD during phase planning`."
    - "The first promoted model scope is `ctrader_xauusd_h1_baseline`, but Bookmap enrichment is allowed through canonical market mappings instead of being excluded globally."
    - "Dataset building makes instrument/timeframe scope explicit and fails or excludes rows when join preconditions are not met."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Official Phase 6 requirement IDs and traceability rows"
      contains: "MLDATA-01"
    - path: "configs/training/instrument_map.json"
      provides: "Canonical mapping between cTrader instruments and Bookmap roots"
      contains: "\"XAUUSD\": [\"GC\"]"
    - path: "main/training/contracts.py"
      provides: "Dataset-scope constants and canonical derived-row schema"
      contains: "ctrader_xauusd_h1_baseline"
    - path: "main/training/build_dataset.py"
      provides: "Deterministic dataset builder with backward-only joins"
      contains: "merge_asof"
    - path: "tests/test_training_join_policy.py"
      provides: "Regression coverage for scope enforcement and backward-only joins"
      contains: "test_builder_excludes_bookmap_scope_from_ctrader_baseline"
  key_links:
    - from: "logs/history_orderflowaggregated_XAUUSD.jsonl"
      to: "main/training/build_dataset.py"
      via: "Order flow is the baseline anchor series for Phase 6"
      pattern: "order_flow_aggregated"
    - from: "logs/history_alertlistener_ESM6.CME_RITHMIC.jsonl"
      to: "main/training/contracts.py"
      via: "Bookmap data joins only after canonical alias mapping, so `ES*` enriches `US500` and not `XAUUSD`"
      pattern: "\"US500\": [\"ES\"]"
---

<objective>
Create the Phase 6 dataset contract and baseline training scope.

Purpose: no model training should begin until the repo can prove which logs belong in the baseline dataset, which do not, and how derived training rows are constructed.
Output: official Phase 6 requirement IDs, one canonical dataset contract, and a deterministic dataset builder for the cTrader `XAUUSD` baseline scope.
</objective>

<execution_context>
@C:/Users/hoang/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/hoang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-build-and-train-ai-trading-model-from-bookmap-and-ctrader-jsonl-logs/06-RESEARCH.md
@.planning/phases/06-build-and-train-ai-trading-model-from-bookmap-and-ctrader-jsonl-logs/06-VALIDATION.md
@main/ai_analyzer.py
@main/runtime_paths.py
@main/requirements.txt
@tests/test_runtime_artifacts.py
@logs/history_alertlistener_ESM6.CME_RITHMIC.jsonl
@logs/history_orderflowaggregated_XAUUSD.jsonl
@logs/history_volumeprofile_XAUUSD.jsonl
@logs/history_wyckoff_XAUUSD.jsonl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Formalize Phase 6 requirement IDs and lock the baseline dataset scope</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, configs/training/instrument_map.json, docs/ai-model-data-contract.md, main/training/__init__.py, main/training/contracts.py, tests/test_training_dataset_contract.py</files>
  <read_first>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/06-build-and-train-ai-trading-model-from-bookmap-and-ctrader-jsonl-logs/06-RESEARCH.md, main/ai_analyzer.py, main/runtime_paths.py, main/requirements.txt</read_first>
  <action>Update `.planning/REQUIREMENTS.md` to add these exact official Phase 6 requirement IDs and descriptions: `MLDATA-01` = raw JSONL logs are cataloged and normalized into a documented deterministic training dataset contract; `MLDATA-02` = dataset building enforces explicit scope, instrument compatibility, canonical instrument mapping, and backward-only joins; `MLLABEL-01` = labels are generated from local historical price movement without future leakage; `MLTRAIN-01` = offline training produces versioned artifacts and metrics from local data only; `MLTRAIN-02` = promotion into `runtime/model.pkl` is explicit and separate from training; `MLEVAL-01` = walk-forward evaluation outputs are reproducible enough to replay and compare safely. Update `.planning/ROADMAP.md` so Phase 6 `Requirements` becomes `[MLDATA-01, MLDATA-02, MLLABEL-01, MLTRAIN-01, MLTRAIN-02, MLEVAL-01]`. Create `configs/training/instrument_map.json` with these exact baseline entries: `"XAUUSD": ["GC"]` and `"US500": ["ES"]`. Create `docs/ai-model-data-contract.md` that states the first promoted scope is exactly `ctrader_xauusd_h1_baseline`, the anchor series is exactly `order_flow_aggregated` from `logs/history_orderflowaggregated_XAUUSD.jsonl`, and Bookmap events may enrich a row only after alias-root mapping through `instrument_map.json`, for example `XAUUSD -> GC*` and `US500 -> ES*`. Create `main/training/contracts.py` with `DATASET_SCOPE_CTRADER_XAUUSD_H1_BASELINE = "ctrader_xauusd_h1_baseline"`, `CANONICAL_MARKET_MAP = {"XAUUSD": ("GC",), "US500": ("ES",)}`, and a derived-row schema that includes `row_id`, `dataset_scope`, `instrument`, `canonical_market`, `anchor_timestamp`, `open`, `high`, `low`, `close`, `spread`, `tick_size`, `has_volume_profile`, `has_wyckoff_window`, `has_bookmap_window`, and `quarantined_source_count`. Create `tests/test_training_dataset_contract.py` with exact tests `test_phase_6_requirement_ids_are_mapped`, `test_contract_locks_ctrader_xauusd_baseline_scope`, `test_contract_maps_xauusd_to_gc_bookmap_root`, and `test_contract_maps_us500_to_es_bookmap_root`.</action>
  <verify>
    <automated>& "$env:LOCALAPPDATA\Programs\Python\Python312\python.exe" -m pytest tests/test_training_dataset_contract.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains `MLDATA-01`
    - `.planning/ROADMAP.md` contains `[MLDATA-01, MLDATA-02, MLLABEL-01, MLTRAIN-01, MLTRAIN-02, MLEVAL-01]`
    - `docs/ai-model-data-contract.md` contains `ctrader_xauusd_h1_baseline`
    - `configs/training/instrument_map.json` contains `"XAUUSD": ["GC"]`
    - `configs/training/instrument_map.json` contains `"US500": ["ES"]`
    - `docs/ai-model-data-contract.md` contains `XAUUSD -> GC`
    - `docs/ai-model-data-contract.md` contains `US500 -> ES`
    - `main/training/contracts.py` contains `DATASET_SCOPE_CTRADER_XAUUSD_H1_BASELINE = "ctrader_xauusd_h1_baseline"`
    - `main/training/contracts.py` contains `CANONICAL_MARKET_MAP = {"XAUUSD": ("GC",), "US500": ("ES",)}`
    - `tests/test_training_dataset_contract.py` contains `test_contract_maps_us500_to_es_bookmap_root`
  </acceptance_criteria>
  <done>The phase has explicit requirement IDs and one locked promoted-model dataset scope.</done>
</task>

<task type="auto">
  <name>Task 2: Build the deterministic dataset builder with explicit backward-only joins</name>
  <files>main/training/build_dataset.py, scripts/build_training_dataset.py, tests/test_training_join_policy.py</files>
  <read_first>main/training/contracts.py, docs/ai-model-data-contract.md, logs/history_orderflowaggregated_XAUUSD.jsonl, logs/history_volumeprofile_XAUUSD.jsonl, logs/history_wyckoff_XAUUSD.jsonl, logs/history_alertlistener_ESM6.CME_RITHMIC.jsonl, .planning/phases/06-build-and-train-ai-trading-model-from-bookmap-and-ctrader-jsonl-logs/06-RESEARCH.md</read_first>
  <action>Create `main/training/build_dataset.py` so it reads the raw JSONL histories, anchors rows on the `XAUUSD` `order_flow_aggregated` H1 series, and writes one derived parquet dataset plus manifests under `artifacts/datasets/ctrader_xauusd_h1_baseline/{run_id}/`. Implement these exact join rules: join `volume_profile` by same `instrument` with backward `merge_asof` and `tolerance = 2h`; aggregate `wyckoff_state` rows where `instrument == "XAUUSD"` and `anchor_timestamp - 1h < timestamp <= anchor_timestamp`; map Bookmap aliases through `instrument_map.json` so aliases rooted at `GC` join `XAUUSD` rows and aliases rooted at `ES` join `US500` rows; aggregate mapped Bookmap events where `anchor_timestamp - 300s < timestamp <= anchor_timestamp` into fixed features `bookmap_event_count_300s`, `bookmap_dot_count_300s`, `bookmap_stop_count_300s`, `bookmap_wall_count_300s`, and `bookmap_signed_value_300s`; record unmatched Bookmap rows in the manifest with `excluded_reason = "canonical_market_mismatch"`. The derived dataset must include the exact fields `row_id`, `dataset_scope`, `instrument`, `canonical_market`, `anchor_timestamp`, `anchor_timeframe`, `orderflow_event_id`, `volumeprofile_event_id`, `wyckoff_window_start`, `wyckoff_window_end`, `open`, `high`, `low`, `close`, `spread`, `tick_size`, `level_count`, `total_volume`, `buy_volume`, `sell_volume`, `delta_sum`, `abs_delta_sum`, `poc_price`, `poc_distance_to_close_ticks`, `wy_count`, `wy_last_direction_sign`, `wy_last_wave_volume`, `wy_sum_wave_volume`, `wy_mean_volume_per_time`, `wy_max_wave_efficiency`, `has_volume_profile`, `has_wyckoff_window`, `has_bookmap_window`, `bookmap_event_count_300s`, `bookmap_dot_count_300s`, `bookmap_stop_count_300s`, `bookmap_wall_count_300s`, `bookmap_signed_value_300s`, and `quarantined_source_count`. Create `scripts/build_training_dataset.py` with the CLI `python scripts/build_training_dataset.py --scope ctrader_xauusd_h1_baseline --logs-dir logs --output-root artifacts/datasets`. Create `tests/test_training_join_policy.py` with exact tests `test_builder_uses_orderflow_as_anchor_series`, `test_builder_uses_backward_only_volume_profile_join`, `test_builder_maps_gc_aliases_into_xauusd_scope`, and `test_builder_rejects_es_aliases_for_xauusd_scope`.</action>
  <verify>
    <automated>& "$env:LOCALAPPDATA\Programs\Python\Python312\python.exe" -m pytest tests/test_training_join_policy.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `main/training/build_dataset.py` contains `merge_asof`
    - `main/training/build_dataset.py` contains `tolerance=pd.Timedelta("2h")`
    - `main/training/build_dataset.py` contains `excluded_reason = "canonical_market_mismatch"`
    - `main/training/build_dataset.py` contains `artifacts/datasets/ctrader_xauusd_h1_baseline`
    - `main/training/build_dataset.py` contains `bookmap_signed_value_300s`
    - `scripts/build_training_dataset.py` contains `--scope ctrader_xauusd_h1_baseline`
    - `tests/test_training_join_policy.py` contains `test_builder_maps_gc_aliases_into_xauusd_scope`
    - `tests/test_training_join_policy.py` contains `test_builder_rejects_es_aliases_for_xauusd_scope`
  </acceptance_criteria>
  <done>The repo can derive one deterministic cTrader baseline dataset without silently fusing unrelated Bookmap data.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify dataset scope and join policy on the real logs</name>
  <files>docs/ai-model-data-contract.md, main/training/build_dataset.py, scripts/build_training_dataset.py</files>
  <read_first>docs/ai-model-data-contract.md, .planning/phases/06-build-and-train-ai-trading-model-from-bookmap-and-ctrader-jsonl-logs/06-RESEARCH.md, .planning/phases/06-build-and-train-ai-trading-model-from-bookmap-and-ctrader-jsonl-logs/06-VALIDATION.md</read_first>
  <action>Pause after Task 2. Run the dataset builder against the real `logs/` directory, inspect the generated manifest under `artifacts/datasets/ctrader_xauusd_h1_baseline/{run_id}/`, and confirm that Bookmap rows are handled through canonical market mapping rules before continuing to training work. For the current repo sample, `ESM6` should be reported as excluded because it maps to `US500/ES`, not `XAUUSD/GC`.</action>
  <verify>
    <automated>python scripts/build_training_dataset.py --scope ctrader_xauusd_h1_baseline --logs-dir logs --output-root artifacts/datasets</automated>
  </verify>
  <what-built>One deterministic baseline dataset scope and one manifest that proves what was included or excluded from training.</what-built>
  <how-to-verify>
    1. Run `python scripts/build_training_dataset.py --scope ctrader_xauusd_h1_baseline --logs-dir logs --output-root artifacts/datasets`.
    2. Open the newest `artifacts/datasets/ctrader_xauusd_h1_baseline/{run_id}/dataset_manifest.json`.
    3. Confirm it contains `excluded_reason` with value `canonical_market_mismatch` for the current `ESM6` Bookmap scope.
    4. Confirm the derived rows belong only to `XAUUSD`.
    5. Confirm the manifest documents the canonical mapping examples `XAUUSD -> GC` and `US500 -> ES`.
    6. Reply `approved` to continue to Plan 02, or describe the mismatch that must be corrected first.
  </how-to-verify>
  <acceptance_criteria>
    - `dataset_manifest.json` exists after the builder command runs
    - `dataset_manifest.json` contains `canonical_market_mismatch`
    - The derived dataset rows use only `XAUUSD`
    - `dataset_manifest.json` contains `XAUUSD`
    - `dataset_manifest.json` contains `GC`
  </acceptance_criteria>
  <resume-signal>Type `approved` or describe the dataset-scope issue that must be fixed before training starts.</resume-signal>
  <done>The dataset contract is proven against the real repo logs before model training begins.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `& "$env:LOCALAPPDATA\Programs\Python\Python312\python.exe" -m pytest tests/test_training_dataset_contract.py -q` succeeds
- [ ] `& "$env:LOCALAPPDATA\Programs\Python\Python312\python.exe" -m pytest tests/test_training_join_policy.py -q` succeeds
- [ ] `python scripts/build_training_dataset.py --scope ctrader_xauusd_h1_baseline --logs-dir logs --output-root artifacts/datasets` succeeds
- [ ] The generated dataset manifest reports current `ESM6` exclusion with `canonical_market_mismatch`
- [ ] `gitnexus_detect_changes({scope: "all", repo: "quant-trading"})` reports only the files expected by this plan before commit
</verification>

<success_criteria>
- All tasks completed
- Phase 6 requirement IDs are formalized
- The promoted baseline scope is explicit
- Dataset construction is deterministic and uses backward-only joins
</success_criteria>

<output>
After completion, create `.planning/phases/06-build-and-train-ai-trading-model-from-bookmap-and-ctrader-jsonl-logs/06-01-SUMMARY.md`
</output>
