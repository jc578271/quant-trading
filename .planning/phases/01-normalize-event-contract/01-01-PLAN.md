---
phase: 01-normalize-event-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/event-contract-v1.md
  - bookmap-addons/src/main/java/com/bookmap/alertlistener/AlertListener.java
autonomous: true
requirements:
  - DATA-01
user_setup: []
must_haves:
  truths:
    - "Bookmap alert, dom, dot, and wall events share one documented v1 envelope."
    - "AlertListener emits canonical envelope fields before sending data to the Python socket."
  artifacts:
    - path: "docs/event-contract-v1.md"
      provides: "Versioned Phase 1 contract reference"
      min_lines: 60
      contains: "schema: event-contract/v1"
    - path: "bookmap-addons/src/main/java/com/bookmap/alertlistener/AlertListener.java"
      provides: "Bookmap contract emitter"
      contains: "sendContractEvent("
  key_links:
    - from: "bookmap-addons/src/main/java/com/bookmap/alertlistener/AlertListener.java"
      to: "docs/event-contract-v1.md"
      via: "implemented envelope keys and event taxonomy"
      pattern: "event-contract/v1"
---

<objective>
Define the v1 contract in-repo and make the Bookmap producer emit it.

Purpose: DATA-01 only becomes durable if the contract lives in the repo and the most active producer is updated to match it.
Output: A versioned contract document plus an AlertListener emitter that sends Bookmap events inside the shared envelope.
</objective>

<execution_context>
@C:/Users/hoang/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/hoang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-normalize-event-contract/01-CONTEXT.md
@.planning/phases/01-normalize-event-contract/01-RESEARCH.md
@bookmap-addons/src/main/java/com/bookmap/alertlistener/AlertListener.java
@ctrader-projects/OrderFlowAggregatedV20/src/Order Flow Aggregated v2.0.cs
@ctrader-projects/FreeVolumeProfileV20/src/Free Volume Profile v2.0.cs
@ctrader-projects/WeisWyckoffSystemV20/src/Weis & Wyckoff System v2.0.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the versioned v1 contract document</name>
  <files>docs/event-contract-v1.md</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/phases/01-normalize-event-contract/01-CONTEXT.md, .planning/phases/01-normalize-event-contract/01-RESEARCH.md, bookmap-addons/src/main/java/com/bookmap/alertlistener/AlertListener.java, ctrader-projects/OrderFlowAggregatedV20/src/Order Flow Aggregated v2.0.cs, ctrader-projects/FreeVolumeProfileV20/src/Free Volume Profile v2.0.cs, ctrader-projects/WeisWyckoffSystemV20/src/Weis & Wyckoff System v2.0.cs</read_first>
  <action>Create `docs/event-contract-v1.md` and declare the producer-required envelope fields exactly as `schema`, `source`, `source_instance`, `event`, `event_id`, `instrument`, `timestamp`, `payload`, and `source_meta`. Document `schema: event-contract/v1`, the event taxonomy `alert`, `dom`, `dot`, `wall`, `order_flow_aggregated`, `volume_profile`, `wyckoff_state`, the rule that `received_at` is added by the Python boundary, the canonical instrument rule favoring venue-qualified identity, and the quarantine triggers `missing instrument`, `missing timestamp`, `unsupported event`, and `schema mismatch`. Include at least one JSON example for a Bookmap event and one for a cTrader event.</action>
  <verify>Select-String -Path docs\event-contract-v1.md -Pattern 'event-contract/v1|wyckoff_state|received_at|missing instrument|order_flow_aggregated'</verify>
  <acceptance_criteria>
    - `docs/event-contract-v1.md` contains `schema: event-contract/v1`
    - `docs/event-contract-v1.md` contains `event taxonomy`
    - `docs/event-contract-v1.md` contains `wyckoff_state`
    - `docs/event-contract-v1.md` contains `received_at is added by the Python boundary`
    - `docs/event-contract-v1.md` contains `missing instrument`
  </acceptance_criteria>
  <done>The repo contains one versioned contract document that names the exact envelope keys and every in-scope event family.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor AlertListener socket sends to one contract emitter</name>
  <files>bookmap-addons/src/main/java/com/bookmap/alertlistener/AlertListener.java</files>
  <read_first>bookmap-addons/src/main/java/com/bookmap/alertlistener/AlertListener.java, docs/event-contract-v1.md</read_first>
  <action>Add a `sendContractEvent(...)` helper in `AlertListener.java` that writes the v1 envelope with `schema = "event-contract/v1"`, `source = "bookmap"`, `source_instance = "AlertListener"`, `event_id`, `instrument`, `timestamp`, `payload`, and `source_meta`. Update `sendDomEvent`, `sendWallEvent`, `sendDotEvent`, and `exportAlert` to call that helper instead of writing flat JSON strings. For DOM, dot, and wall events use the current `alias` as both `instrument` and `source_meta.alias`; for alerts use the current `symbol` as `instrument` and keep the original `symbol`, `text`, `count`, and `popup` inside `payload` or `source_meta`. Stamp a timestamp for all four Bookmap event families so none of them rely on the Python side to invent event time.</action>
  <verify>Select-String -Path bookmap-addons\src\main\java\com\bookmap\alertlistener\AlertListener.java -Pattern 'sendContractEvent\\(|event-contract/v1|source_instance|event_id|payload|source_meta'</verify>
  <acceptance_criteria>
    - `bookmap-addons/src/main/java/com/bookmap/alertlistener/AlertListener.java` contains `sendContractEvent(`
    - `bookmap-addons/src/main/java/com/bookmap/alertlistener/AlertListener.java` contains `event-contract/v1`
    - `bookmap-addons/src/main/java/com/bookmap/alertlistener/AlertListener.java` contains `"bookmap"`
    - `bookmap-addons/src/main/java/com/bookmap/alertlistener/AlertListener.java` contains `source_instance`
    - `bookmap-addons/src/main/java/com/bookmap/alertlistener/AlertListener.java` contains `event_id`
  </acceptance_criteria>
  <done>All Bookmap socket paths emit the documented envelope instead of separate flat JSON shapes.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild the Bookmap addon after the contract refactor</name>
  <files>bookmap-addons/src/main/java/com/bookmap/alertlistener/AlertListener.java</files>
  <read_first>bookmap-addons/gradlew.bat, bookmap-addons/build.gradle, bookmap-addons/src/main/java/com/bookmap/alertlistener/AlertListener.java</read_first>
  <action>From `bookmap-addons`, run `$env:JAVA_HOME = "C:\\Program Files\\Bookmap\\jre"; .\\gradlew.bat alertListenerJar` to confirm the refactored AlertListener still builds into a jar. If the build fails, fix only contract-related compile errors introduced by this plan.</action>
  <verify>Get-ChildItem bookmap-addons\build\libs\*.jar</verify>
  <acceptance_criteria>
    - `bookmap-addons\build\libs\*.jar` returns at least one file
    - The `alertListenerJar` command exits `0`
  </acceptance_criteria>
  <done>The Bookmap producer changes compile successfully and produce an updated jar artifact.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `Select-String -Path docs\event-contract-v1.md -Pattern 'event-contract/v1|wyckoff_state|missing instrument'` returns matches
- [ ] `Select-String -Path bookmap-addons\src\main\java\com\bookmap\alertlistener\AlertListener.java -Pattern 'sendContractEvent\\(|event_id|source_meta'` returns matches
- [ ] `$env:JAVA_HOME = "C:\\Program Files\\Bookmap\\jre"; Set-Location .\bookmap-addons; .\gradlew.bat alertListenerJar` succeeds
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Bookmap alert, dom, dot, and wall events are described and emitted through one v1 envelope
  </success_criteria>

<output>
After completion, create `.planning/phases/01-normalize-event-contract/01-01-SUMMARY.md`
</output>
