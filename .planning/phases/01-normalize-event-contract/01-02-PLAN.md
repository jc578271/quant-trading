---
phase: 01-normalize-event-contract
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - main/event_contract.py
  - main/socket_server.py
  - main/ai_analyzer.py
autonomous: true
requirements:
  - DATA-02
user_setup: []
must_haves:
  truths:
    - "The Python ingest path rejects malformed or version-mismatched events without stopping the socket loop."
    - "Rejected records preserve both reason and raw payload in a quarantine artifact."
    - "DOM events no longer crash because `self.order_book` is initialized and fed canonical data."
  artifacts:
    - path: "main/event_contract.py"
      provides: "Contract normalization and rejection rules"
      min_lines: 60
      contains: "SCHEMA_VERSION = \"event-contract/v1\""
    - path: "main/socket_server.py"
      provides: "Boundary validation and quarantine logging"
      contains: "quarantine_events.jsonl"
    - path: "main/ai_analyzer.py"
      provides: "Canonical envelope consumer"
      contains: "self.order_book = {}"
  key_links:
    - from: "main/socket_server.py"
      to: "main/event_contract.py"
      via: "normalize before callback"
      pattern: "normalize_record"
    - from: "main/ai_analyzer.py"
      to: "main/event_contract.py"
      via: "consumes canonical event and payload fields"
      pattern: "data.get\\(\"event\""
---

<objective>
Make the Python boundary enforce the v1 contract visibly and safely.

Purpose: DATA-02 requires malformed payloads to be rejected without crashing the ingest loop, and the current analyzer already has contract bugs that must be fixed at the same time.
Output: A normalization module, quarantine logging in the socket server, and an analyzer that reads canonical envelopes.
</objective>

<execution_context>
@C:/Users/hoang/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/hoang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-normalize-event-contract/01-CONTEXT.md
@.planning/phases/01-normalize-event-contract/01-RESEARCH.md
@docs/event-contract-v1.md
@main/socket_server.py
@main/ai_analyzer.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Python contract normalizer</name>
  <files>main/event_contract.py</files>
  <read_first>docs/event-contract-v1.md, main/socket_server.py, main/ai_analyzer.py</read_first>
  <action>Create `main/event_contract.py` with `SCHEMA_VERSION = "event-contract/v1"` and a `normalize_record(record, received_at)` function that accepts either a documented v1 envelope or the currently known legacy flat shapes during rollout. Normalize legacy Bookmap `type` values `alert`, `dom`, `dot`, and `wall`; normalize legacy cTrader payloads `order_flow_aggregated` and `volume_profile`; map the current Weis/Wyckoff payload with no `type` to `event = "wyckoff_state"` when it contains `wyckoffVolume` or `waveVolume`. Require `instrument`, `timestamp`, and a supported `event`, preserve raw source identity under `source_meta`, and return a rejection reason string for `missing instrument`, `missing timestamp`, `unsupported event`, or `schema mismatch`.</action>
  <verify>Select-String -Path main\event_contract.py -Pattern 'SCHEMA_VERSION = \"event-contract/v1\"|def normalize_record|missing instrument|unsupported event|wyckoff_state'</verify>
  <acceptance_criteria>
    - `main/event_contract.py` contains `SCHEMA_VERSION = "event-contract/v1"`
    - `main/event_contract.py` contains `def normalize_record`
    - `main/event_contract.py` contains `missing instrument`
    - `main/event_contract.py` contains `unsupported event`
    - `main/event_contract.py` contains `wyckoff_state`
  </acceptance_criteria>
  <done>The workspace has one Python module that turns both v1 and known legacy inputs into the canonical envelope or a precise rejection reason.</done>
</task>

<task type="auto">
  <name>Task 2: Add validation and quarantine at the socket boundary</name>
  <files>main/socket_server.py</files>
  <read_first>main/socket_server.py, main/event_contract.py, docs/event-contract-v1.md</read_first>
  <action>Patch `main/socket_server.py` to call `normalize_record(record, received_at)` immediately after `json.loads`. If normalization fails, append one JSON line to `quarantine_events.jsonl` with `received_at`, `reason`, and the original `raw` record, log `Rejected event:` plus the reason, and continue listening. If normalization succeeds, pass only the canonical envelope to the callback. Keep malformed JSON logging, but distinguish JSON parse failure from contract rejection in the logs.</action>
  <verify>Select-String -Path main\socket_server.py -Pattern 'normalize_record|quarantine_events.jsonl|Rejected event:|received_at|reason'</verify>
  <acceptance_criteria>
    - `main/socket_server.py` contains `normalize_record`
    - `main/socket_server.py` contains `quarantine_events.jsonl`
    - `main/socket_server.py` contains `Rejected event:`
    - `main/socket_server.py` contains `received_at`
    - `main/socket_server.py` contains `reason`
  </acceptance_criteria>
  <done>The socket server visibly rejects invalid contract records while keeping the ingest loop alive.</done>
</task>

<task type="auto">
  <name>Task 3: Teach AIAnalyzer to consume canonical envelopes and fix DOM state setup</name>
  <files>main/ai_analyzer.py</files>
  <read_first>main/ai_analyzer.py, main/event_contract.py, docs/event-contract-v1.md</read_first>
  <action>Initialize `self.order_book = {}` in `AIAnalyzer.__init__`. In `process_data`, read `event = data.get("event", data.get("type", "indicator"))`, `payload = data.get("payload", data)`, and prefer `instrument` over `symbol` when choosing the buffer key. Keep alert, dom, dot, and wall branches working by reading `action`, `alias`, `isBid`, `isBuy`, `price`, `size`, and `duration` from `payload` first, then legacy top-level fallback if needed. For indicator-style events, export and buffer a merged dictionary that keeps canonical metadata plus the payload fields so downstream feature extraction still sees `deltaRank`, `vpPOC`, `waveVolume`, `waveDirection`, and `spread`.</action>
  <verify>Select-String -Path main\ai_analyzer.py -Pattern 'self.order_book = \\{\\}|data.get\\(\"event\"|payload = data.get\\(\"payload\"|instrument'</verify>
  <acceptance_criteria>
    - `main/ai_analyzer.py` contains `self.order_book = {}`
    - `main/ai_analyzer.py` contains `data.get("event", data.get("type", "indicator"))`
    - `main/ai_analyzer.py` contains `payload = data.get("payload", data)`
    - `main/ai_analyzer.py` contains `instrument`
  </acceptance_criteria>
  <done>The analyzer can safely consume canonical envelopes and no longer crashes on DOM because of missing order-book initialization.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `Select-String -Path main\event_contract.py -Pattern 'event-contract/v1|normalize_record|wyckoff_state'` returns matches
- [ ] `Select-String -Path main\socket_server.py -Pattern 'quarantine_events.jsonl|Rejected event:|normalize_record'` returns matches
- [ ] `Select-String -Path main\ai_analyzer.py -Pattern 'self.order_book = \\{\\}|data.get\\(\"event\"|payload = data.get\\(\"payload\"'` returns matches
- [ ] `python -m compileall main` succeeds
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Invalid events are quarantined visibly and valid events reach the analyzer in one canonical envelope
  </success_criteria>

<output>
After completion, create `.planning/phases/01-normalize-event-contract/01-02-SUMMARY.md`
</output>
